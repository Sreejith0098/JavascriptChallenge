//Find the first non-repeating element in the array.

function nonRepeatingDigit(){
       let array = [1,3,1,4,5,4,5]

       for(i=0; i<array.length; i++){
        count = 0;
        for(j=0; i<array.length; j++){
           
            }
        } if(array[i] === array[j]){
            count++
       } if(array[i]===1){
        console.log(`the first non repeaiting digit is ${array[i]}`)
       } 
}

nonRepeatingDigit()